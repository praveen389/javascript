// primitive vs ReferenceError

// javascript has 2 diffenent types of values:

// a. primitive values

// b. reference value 

primitive value => undefined , null , boolean , number , string.boolean

reference values => objects

// javascript stores objects ans function on the heap data structuredClone

// statis data has a size thet does not change so js engine allows amount of memory space of the static data and store on the stack data 

